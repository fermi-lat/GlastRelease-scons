package Event
version v8r13p2

branches Event cmt doc src
use idents v2r*

use GaudiKernel v11r0p*
use geometry    v3*
use CLHEP v1r7p*


apply_pattern package_include
apply_pattern package_linkopts


apply_pattern package_headers

macro_append Event_headers "" WIN32 " \
        ../Event/*.h \ 
        ../Event/TopLevel/*.h \
        ../Event/Utilities/*.h \
        ../Event/MonteCarlo/*.h \
        ../Event/Digi/*.h \
        ../Event/Recon/*.h \
        ../Event/Recon/TkrRecon/*.h\
        ../Event/Recon/CalRecon/*.h\
        ../Event/RelTable/*.h

library Event -no_share\
        MonteCarlo/*.cxx \
        Recon/TkrRecon/*.cxx \
        Recon/CalRecon/*.cxx \
        TopLevel/*.cpp \
        ${Event_headers}


macro GlastEvent_stamps   "${EventDir)/Event.stamp"

# simple test program
# use this when we want to make a Gaudi test program apply_pattern package_test

application test_Event test/testmain.cxx

application test_Tables test/test_RelTabs.cxx
