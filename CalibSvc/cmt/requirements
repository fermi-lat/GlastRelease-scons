# $Header$
### Might end up changing this name to have a prefix a bit more general
### than "Calib"; e.g., DetSvc or CondSvc
package CalibSvc
version v0r0p0

apply_pattern packageShr

use CalibData v0r*
use calibUtil v0r*


use GaudiInterface * IExternal

apply_pattern package_include
apply_pattern package_headers
## apply_pattern package_linkopts

library CalibSvc *.cxx  CalibDataSvc/*.cxx      \
                        CalibMySQLCnv/*.cxx     \
                        CalibXMLCnv/*.cxx       \
                        CalibXMLCnv/cnv/*.cxx   \
                        Dll/*.cxx ${CalibSvc_headers}

apply_pattern package_Cshlibflags

private
macro WinInclude "" WIN32 "test/*.h test/*.txt"
use GlastSvc v9r*
use RootIo   v6r*


public
set JOBOPTIONS "${CALIBSVCROOT}/src/test/jobOptions.txt"
application simpletest ${GlastMain} test/*.cxx $(WinInclude)

