package CalRecon
version v5r17p1

author Alexandre Chekhtman <chehtman@gamma.nrl.navy.mil>
# $Id$

branches src cmt doc

use GuiSvc v3*
use Event *
use GlastSvc *
use CalUtil v1r*
use geometry v3*
use CalibSvc v*
use TkrUtil *
use xmlBase *

# The following two lines are needed to use the FuzzyCluster package
#use FuzzyCluster v*
#use CLHEP * IExternal

apply_pattern packageShr

### hide the packages used only for test program
private
use RootPolicy
use RootIo v*

macro_append ROOT_linkopts " $(ROOT_GUI_libs)"
macro_append ROOT_linkopts " -L${ROOT_PATH}/lib -lMinuit " \ 
                 VisualC " ${ROOT_PATH}/lib/libMinuit.lib " 

apply_pattern package_headers

library CalRecon  *.cxx *.cpp Dll/*.cpp ${CalRecon_headers}


macro CalRecon_stamps     

private

apply_pattern package_Cshlibflags 
set JOBOPTIONS ${CALRECONROOT}/src/test/jobOptions.txt

# build this for testing, console only. Load all objects directly. 

macro testOptions  ""  VisualC   "test/jobOptions.txt"

application test_CalRecon $(GlastMain) test/*.cxx $(testOptions)
