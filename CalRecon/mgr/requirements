package CalRecon
version v1

branches src mgr doc

use GlastSvc

use reconstruction v3r1

library CalRecon  *.cxx ../CalRecon/*.h  Dll/*.cpp

macro CalRecon Dir  "$(CALRECONROOT)/$(DEBUGDIR)"

macro CalRecon_linkopts   " " 
macro CalRecon_stamps     

set   CalReconShr           "${CALRECONROOT}/${DEBUGDIR}/libCalRecon"

include_path $(CALRECONROOT)

private

# this for MSVCC, to fix incompatibilities, turn off warnings
macro_append cppflags ""\
       VisualC " /FI$(ntconfigfile)" 

# note that the debug versions of the used libraries are inserted below

macro CalRecon_shlibflags   "$(componentshr_linkopts) $(use_linkopts)"\
      VisualC               "$(componentshr_linkopts) $(use_linkopts) $(Win32Debug)"

# build this for testing

#application test \
#        test/*.cxx test/*.h test/*.txt

application test \
        test/*.cxx

