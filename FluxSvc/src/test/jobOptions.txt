//##############################################################
//
// Job options file for FluxSvc test program
//


ApplicationMgr.ExtSvc   = { "FluxSvc" };

ApplicationMgr.DLLs   = { "FluxSvc" }; 

//--------------------------------------------------------------
// Private Application Configuration options
//--------------------------------------------------------------

ApplicationMgr.TopAlg = { "FluxTestAlg" };

// This is the default file that gets sent to the parser
FluxSvc.source_lib_default="$(FLUXROOT)/xml/source_library.xml";

// Here is a list of files (can be empty) that also is parsed
FluxSvc.source_lib = {"$(FLUXROOT)/xml/user_library.xml"};

// Set output level threshold (2=DEBUG, 3=INFO, 4=WARNING, 5=ERROR, 6=FATAL )

MessageSvc.OutputLevel      = 2;
FluxTestAlg.source_name="gamma_pencil";

//--------------------------------------------------------------
// Algorithms Private Options
//--------------------------------------------------------------

ApplicationMgr.EvtSel = "NONE";
ApplicationMgr.EvtMax = 100;

//==============================================================
//
// End of job options file
//
//##############################################################

